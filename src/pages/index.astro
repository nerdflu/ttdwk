---
import CityShell from '../components/CityShell.astro';
import { websiteSchema, organizationSchema } from '../lib/schema';
import { getCities } from '../lib/data';

const cities = getCities();
const jsonLd = [websiteSchema(), organizationSchema()];
---
<CityShell title="Things to Do With Kids in Australia | Family Activities" description="Discover the best family activities, events, and places to go with kids." jsonLd={jsonLd}>
  <div style="padding: 20px;">
    <h1>Things to do with kids in Australia</h1>
    
    <section>
      <h2>Browse family activities by city</h2>
      <ul>
        {cities.map((city: any) => (
          <li><a href={"/" + city.slug + "/"}>{city.title}</a></li>
        ))}
      </ul>
    </section>
  </div>
</CityShell>
